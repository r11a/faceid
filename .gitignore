# local configuration (contains MQTT credentials)
/config.yaml

# runtime data: face gallery, unknown queue
data/

# python
venv/
__pycache__/
*.pyc

# frontend dependencies (the production bundle is committed under static/)
node_modules/

# insightface model cache
.insightface/
